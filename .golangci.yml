version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosimple
    - gocritic
    - misspell
    - unconvert
    - unparam
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
